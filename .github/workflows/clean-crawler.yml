name: Check In

# 签到续期：运行此 workflow 可重置 7 天计时，保持 Get Hot News 正常运行
#
# 操作方法：
#   1. 点击 Run workflow 按钮
#   2. 每 7 天内至少运行一次

on:
  workflow_dispatch:

jobs:
  keep_alive:
    runs-on: ubuntu-latest
    steps:
      - name: Keep alive
        run: echo "Check In OK"
